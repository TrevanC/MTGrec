# Backend Environment Variables for Railway Deployment
# Copy this file and update values as needed

# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/mtg_recommender
# Note: Railway will automatically provide DATABASE_URL when you add PostgreSQL

# Application Configuration
DEBUG=false
API_V1_STR=/api/v1
PROJECT_NAME=MTG EDH Recommender
VERSION=1.0.0

# CORS Configuration
BACKEND_CORS_ORIGINS=https://your-frontend-domain.railway.app,https://your-frontend-domain.railway.app

# Data Files Configuration
DATASET_PATH=/app/data/processed/compact_dataset.json.gz
SIMILARITY_MODEL_PATH=/app/data/processed/similarity_model.pkl

# Optional: Custom port (Railway will override this)
PORT=8000
